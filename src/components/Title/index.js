import React from 'react';
import { Text } from 'react-native';

import commonStyles from 'constants/styles';

const Title = () => <Text style={commonStyles.title}>CODE 777</Text>;

export default Title;
